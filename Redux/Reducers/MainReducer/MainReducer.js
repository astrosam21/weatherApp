const initialState = {
  id: "Hello",
  title: "hello bro",
};
const MainReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default MainReducer;
